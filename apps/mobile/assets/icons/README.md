# Icon assets (reserved)

Reserved for small iconography (condition/medication/device glyphs, the T1D
awareness ribbon, navigation icons). **Empty for Demo 1** — no source icon art was
provided, so screens use text labels and themed pills instead of icon bitmaps.

Add PNGs/SVGs here and register them in `src/assets/maggieAssets.ts` when available.
