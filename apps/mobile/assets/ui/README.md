# UI assets (reserved)

Reserved for non-character UI art (panels, frames, backgrounds, decorative pixel
elements). **Empty for Demo 1** — no source art was provided for these, so the UI
currently renders chunky cards/buttons from the theme (`src/theme/maggieTheme.ts`)
instead of bitmap UI assets.

Add PNGs here and register them in `src/assets/maggieAssets.ts` when available.
